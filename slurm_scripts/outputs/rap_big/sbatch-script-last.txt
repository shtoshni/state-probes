#!/bin/bash
#SBATCH --partition=speech-gpu --cpus-per-task=1 --array=1-6 --job-name=rap_big -o rap_big-%A_%a.out --constraint=a6000
bash -c "`sed "${SLURM_ARRAY_TASK_ID}q;d" batch-commands-last.txt`"
